#!/usr/bin/env python
# _*_ coding: cp1252 _*_
# _*_ cdoing: 850 _*_
# _*_ coding: utf-8 _*_

# El condicional 'If': El bloque de código if va a ejecutar un determinado código solo si se cumplen una o varias condiciones. Las condiciones de los if son booleanas, es decir, tienen dos posibilidades (True or False)
# Operadores de comparación: > Mayor que, < Menor que, >= Mayor o igual que, <= Menor o igual que, == Igual que, != Diferente que

num1 = 10 #Valor de la primer variable
num2 = 20 #Valor de la segunda variable
num3 = 10 #Valor de la tercer variable

if num1 == num2: #Uso del if con el operador de comparción 'igual que'(==), este compara si el valor de num1 es igual al valor de num2
    print('Se ejecuta el if.') #Mensaje a mostrar si el if es verdadero o falso, si es falso no se mostrara nada

if num1 == num3: #Comparamos si el valor de num1 es igual al valor de num3
    print('Se ejecuto el if.') #Mensaje a mostrar si el if es verdadero o falso, en este caso es verdadero

if num1 != num2: #Compara si num1 es diferente de num2
    print('SE EJECUTO EL IF') #Mensaje a mostrar si el if es verdadero o falso